{
  "name": "owlpo-landing",
  "private": true,
  "scripts": {
    "preview": "vercel dev",
    "preview:html": "serve . -l 3456",
    "images": "node scripts/optimize-images.mjs",
    "test": "node --test 'tests/*.test.js'",
    "test:watch": "node --test --watch 'tests/*.test.js'"
  },
  "devDependencies": {
    "serve": "^14.2.4",
    "sharp": "^0.35.1",
    "supabase": "^2.98.2",
    "vercel": "^53.3.2"
  }
}
